const express = require('express');
const router = express.Router();

/* GET api listing. */
router.get('/', (req, res) => {
  res.send('api works');
});

/* GET card selection */

/* GET card details */

module.exports = router;
